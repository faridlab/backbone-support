//! ServiceLevelPriority seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial ServiceLevelPriority data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// SERVICELEVELPRIORITY SEEDER
// ============================================================================

/// Seeder for service_level_priority
pub struct SeedServiceLevelPrioritySeeder;

impl SeedServiceLevelPrioritySeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedServiceLevelPrioritySeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedServiceLevelPrioritySeeder {
    fn name(&self) -> &'static str {
        "SeedServiceLevelPrioritySeeder"
    }

    fn order(&self) -> i32 {
        3
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if support.service_level_priorities table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM support.service_level_priorities")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/service_level_priority_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM support.service_level_priorities")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

